import type{BenchmarkPoint}from'../types/benchmark';export function validatePoint(p:Partial<BenchmarkPoint>){const e:string[]=[];if(!p.system_name?.trim())e.push('Name is required');if(typeof p.accuracy_percent!=='number'||p.accuracy_percent<0||p.accuracy_percent>100)e.push('Accuracy must be between 0 and 100');if(typeof p.cost_usd_per_task!=='number'||p.cost_usd_per_task<=0)e.push('Cost must be greater than zero');return e}export const compatible=(a:BenchmarkPoint,b:BenchmarkPoint)=>a.benchmark===b.benchmark&&a.evaluation_split===b.evaluation_split&&a.metric===b.metric&&a.currency===b.currency&&a.directly_comparable&&b.directly_comparable;
