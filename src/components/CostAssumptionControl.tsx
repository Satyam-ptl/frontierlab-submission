export default function CostAssumptionControl({value,onChange}:{value:number;onChange:(n:number)=>void}){return <fieldset><legend>2. Cost-assumption multiplier</legend><label htmlFor="mult">Scenario: {value.toFixed(2)}x</label><input id="mult" type="range" min="0.25" max="4" step="0.25" value={value} onChange={e=>onChange(+e.target.value)}/><div className="button-row">{[.5,1,2].map(v=><button type="button" key={v} onClick={()=>onChange(v)}>{v}x{v===1?' published estimate':''}</button>)}</div><p className="question">How sensitive is the frontier to cost assumptions?</p></fieldset>}