# Reproducibility

Run `npm install`, `npm run validate:data`, `npm run test`, and `npm run build`. The data snapshot is dated 2026-09-07. Original source values are versioned in JSON and CSV. Scenario adjustments are ephemeral and exported separately. This project reproduces the Pareto mathematics, not the BDH-CQ benchmark run.
