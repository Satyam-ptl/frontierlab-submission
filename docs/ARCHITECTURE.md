# Architecture

Static React + TypeScript + Vite application. JSON is loaded from `public/data`; pure functions in `src/lib` handle validation, dominance, frontier, and efficiency. React state controls filters, scenario multipliers, selection, and one hypothetical challenger. SVG provides the chart, with a synchronized HTML table. No backend, authentication, analytics, model download, or remote inference.
