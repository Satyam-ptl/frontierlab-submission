import{it,expect}from'vitest';import fs from'node:fs';it('major controls have explicit labels',()=>{const a=fs.readFileSync('src/components/CostAssumptionControl.tsx','utf8');expect(a).toContain('htmlFor="mult"');const b=fs.readFileSync('src/components/ChallengerBuilder.tsx','utf8');expect(b).toContain('<button')});it('skip link and chart labels exist',()=>{const a=fs.readFileSync('src/App.tsx','utf8');expect(a).toContain('Skip to content');const b=fs.readFileSync('src/components/ParetoChart.tsx','utf8');expect(b).toContain('aria-labelledby')});
