import{it,expect}from'vitest';import{dominators}from'../lib/dominance';import type{BenchmarkPoint}from'../types/benchmark';const p=(id:string,c:number,a:number)=>({system_id:id,system_name:id,cost_usd_per_task:c,accuracy_percent:a}as BenchmarkPoint);it('multiple dominators',()=>expect(dominators(p('x',3,1),[p('a',1,2),p('b',2,3)])).toHaveLength(2));it('challenger insertion',()=>expect(dominators(p('bdh',.0007,29.5),[p('challenger',.0006,30)])).toHaveLength(1));