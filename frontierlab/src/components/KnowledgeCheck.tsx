import{useState}from'react';export default function KnowledgeCheck(){const[a,setA]=useState('');const choices=['A new model with 80% pass@1 at an unknown cost','A compatible model at $0.0007 and 29.6% pass@2','A cheaper model on a different benchmark','A blog calling another system more intelligent'];return <section className="panel"><h2>Limitation check</h2><p>Which discovery most directly invalidates the displayed claim?</p>{choices.map((x,i)=><label key={x}><input type="radio" name="quiz" value={String(i)} checked={a===String(i)} onChange={e=>setA(e.target.value)}/>{x}</label>)}{a&&<p role="status" className={a==='1'?'survive':'fail'}>{a==='1'?'Correct. It is protocol-compatible, no more expensive, at least as accurate, and strictly better in accuracy.':'Not quite. The challenger must be protocol-compatible and dominate on the two plotted dimensions.'}</p>}</section>}