import type{BenchmarkPoint}from'../types/benchmark';import{efficiency}from'../lib/efficiency';export default function EfficiencyRatioPanel({p}:{p?:BenchmarkPoint}){if(!p)return null;const r=efficiency(p.accuracy_percent,p.adjusted_cost??p.cost_usd_per_task);return <section className="panel"><h2>Ratio versus frontier</h2><p className="metric">{r==null?'Not reported':r.toLocaleString(undefined,{maximumFractionDigits:0})}</p><p>Benchmark percentage points per US dollar</p><p className="warning">This ratio is an educational efficiency measure for the selected benchmark. It is not a general measurement of intelligence and is not equivalent to Pareto optimality.</p></section>}