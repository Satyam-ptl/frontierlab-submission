# Data dictionary

Each point stores identity, benchmark, split, metric, pass protocol, sourced accuracy and cost, currency, methodology/date, evidence category, source, comparability, limitations, and notes. `directly_comparable` gates the strict frontier. `synthetic` prevents teaching examples from being represented as research evidence. Adjusted costs are computed only in browser state and never overwrite this snapshot.
